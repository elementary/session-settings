install_data(
    'pantheon.session',
    install_dir: join_paths(get_option('datadir'), 'gnome-session', 'sessions')
)

install_data(
    'pantheon-wayland.session',
    install_dir: join_paths(get_option('datadir'), 'gnome-session', 'sessions')
)
